# E 组字段表接入说明（V8.3）

本版把 `field_dictionary.csv` 与 `field_gap_register.csv` 接入 Agent 后台，**不在用户首页展示技术表格**。

## 1. 已分析内容
- 字段字典：51 个字段，覆盖地点标准化、Baseline Hazard、Peak-season、Future、Extreme drought、采购 Exposure、材料参数和证据元数据 8 类。
- 缺口登记：10 项缺口，其中 P0=5、P1=4、P2=1；`GAP-001 / GAP-006 / GAP-010` 标记为可能造成 insufficient 的关键缺口。

## 2. 在 Agent 中的用途
- 自动判断企业侧最低输入：原材料、供应节点/地区、采购数量或份额 W。
- 采购权重缺失时停止正式 Baseline，不用公共均值替代。
- 已知采购份额不足 100% 时显式保留 Unknown，不自动归一化。
- 用户询问“需要哪些数据/缺什么字段”时，回答直接来自字段合同与缺口登记。
- 数据完整性检查时在折叠 Trace 中记录“字段合同检查”。
- `/api/data-contract` 供内部 QA 查看字段合同加载状态。
- DeepSeek 可读取经过压缩的字段合同/缺口摘要，用于解释数据要求；不得用这些元数据替代确定性计算。

## 3. 发现的口径差异（未擅自覆盖模型）
- `DR`：E 字段字典标注为 0–5；当前冻结 Baseline 使用 0–1 历史干旱负担。
- `CTS / OA`：E 字典中文释义与研究报告冻结定义不完全一致；当前计算继续使用冻结模型定义。
- Future / Extreme drought：年份、SPEI 时间尺度、阈值最终仍以 E/D 冻结规范为准。

因此，本版将两张表作为**数据治理合同与缺口登记**，而不是作为公式定义文件。
